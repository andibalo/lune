{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "gen:react:web": "turbo gen workspace --copy react-web-base --type app"
  },
  "dependencies": {},
  "devDependencies": {
    "turbo": "^2.5.4"
  },
  "packageManager": "bun@1.2.14",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}